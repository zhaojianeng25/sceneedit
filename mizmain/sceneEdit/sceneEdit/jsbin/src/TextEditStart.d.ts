declare class TextEditStart {
    static initCanvas($caves: HTMLCanvasElement): void;
    private static initmosort;
    static resetSize(): void;
    private static step;
    private static upFrame;
}
